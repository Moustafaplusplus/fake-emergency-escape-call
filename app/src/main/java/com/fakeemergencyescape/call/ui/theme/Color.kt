package com.fakeemergencyescape.call.ui.theme

import androidx.compose.ui.graphics.Color

// Modern vibrant palette — bold, energetic, premium feel
val PrimaryAccent = Color(0xFF00D9FF)      // Vibrant cyan
val PrimaryAccentDark = Color(0xFF0099CC)  // Deep cyan
val PrimaryAccentLight = Color(0xFF66EEFF) // Light cyan
val SecondaryAccent = Color(0xFF9D4EDD)    // Purple
val TertiaryAccent = Color(0xFF3A86FF)     // Electric blue

val AppBackground = Color(0xFFF4F7FB)      // Soft cool white
val AppSurface = Color(0xFFFFFFFF)         // Card white
val AppSurfaceVariant = Color(0xFFE8EDF4)  // Soft gray-blue
val AppOnBackground = Color(0xFF1C2D42)    // Primary text — deep slate (readable on light BG)
val AppOnSurface = Color(0xFF243548)       // Body text on cards
val AppOnSurfaceMuted = Color(0xFF5A6B80)  // Secondary / hint text
val AppTextAccent = Color(0xFF006B8F)      // Accent labels & links (not bright cyan)
val AppOutline = Color(0xFFC5D0DE)         // Soft border

val GlassBackground = Color(0x99FFFFFF)    // Glassmorphic white
val GlassBorder = Color(0x4DFFFFFF)        // Soft glass border

// Modern call chrome
val CallBackgroundTop = Color(0xFF0A1428)      // Deep navy
val CallBackgroundBottom = Color(0xFF1F2D47)   // Navy blue
val CallTextPrimary = Color(0xFFFFFFFF)        // Pure white
val CallTextSecondary = Color(0xFFB3C6FF)      // Soft blue
val CallAnswerGreen = Color(0xFF00D962)        // Modern green
val CallDeclineRed = Color(0xFFFF3B30)         // System red
val CallEndRed = Color(0xFFFF453A)             // Bright red
val CallControlBg = Color(0x26FFFFFF)          // Subtle glass
val CallControlBgActive = Color(0x4DFFFFFF)    // Active glass
val CallAvatarRing = Color(0x4DFFFFFF)         // Bright ring
